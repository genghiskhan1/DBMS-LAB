SET SERVEROUTPUT ON

DROP TABLE EMPLOYEE;

CREATE TABLE EMPLOYEE (
SSN NUMBER(10) PRIMARY KEY,
NAME VARCHAR(10) NOT NULL,
BALANCE NUMBER(10) NOT NULL,
DEPTNO NUMBER(10) NOT NULL
);

INSERT INTO EMPLOYEE VALUES(1,'RAM',10000,10);
INSERT INTO EMPLOYEE VALUES(2,'HARAM',30000,11);
INSERT INTO EMPLOYEE VALUES(3,'GARAM',25000,10);
INSERT INTO EMPLOYEE VALUES(4,'SHARAM',20000,12);

SELECT *FROM EMPLOYEE;

BEGIN
UPDATE EMPLOYEE
SET BALANCE=BALANCE+(BALANCE*0.15)
WHERE DEPTNO=10;
COMMIT;
END;
/

SELECT *FROM EMPLOYEE;
